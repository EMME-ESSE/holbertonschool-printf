#ifndef MAIN_H
#define MAIN_H
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <stdarg.h>
int _printf(const char *format, ...);
int _putchar(char c);
struct dato
{
	char *tipo;
	char *cadena;
};
#endif
